# docker-compose 部署环境变量

# PostgreSQL
POSTGRES_USER=recruit
POSTGRES_PASSWORD=recruit
POSTGRES_DB=recruit

# server: JWT 签名密钥 (生产务必替换为强随机值)
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d

# web 对外源 (server CORS 放行)
WEB_ORIGIN=http://localhost

# web 对外端口
WEB_PORT=80
